// This is a comment

// This is a preprocessor command to include the standard input output library.
#include <stdio.h>

// Entry point for a program. Always the function with name main
int main() // function signature is <type> <func-name>()
{
  /** 
   * Another type of comment, can be multiline.
   */ 
  // printing something here..
  printf("Hello World!!\n"); // another type of comment after the statement. Single line
  return 0; // returning something.. the type of returning value has to match the function signature
}